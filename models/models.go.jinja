//models 数据模型模块
package models

import (
	"{{ app_name }}/models/{{ source }}"

	"github.com/Golang-Tools/sqlhelper"
)

//Init 初始化数据模型模块
// 主要是挂载代理对象的回调
func Init() {
	sqlhelper.DB.Regist({{ source }}.{{ source | title }}RegistCallback)
}
